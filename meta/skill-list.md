# Template Skill List

> Auto-maintained index of every skill bundled in the template (`template/.claude/skills/`).
> Maintained by the `skill-author` authoring skill. Do not edit by hand without re-running that skill —
> regenerate this table whenever a template skill is added, removed, renamed, or its description changes.

| Skill | Description |
|-------|-------------|
| `add-domain` | Bootstrap a new business domain or bounded context with scaffolding, architecture-doc updates, and structural tests that protect the new boundary. |
| `bug-fix` | Reproduce and fix reported bugs with test-first verification and root-cause analysis; reproduce before fixing, fix the cause not the symptom. |
| `doc-gardening` | Scan the repository for stale, broken, or missing documentation and repair the highest-priority issues, reconciling docs with the actual code. |
| `meta` | Capture reusable agent behavior as a skill update or new skill after a task completes, then refine it against the skill guide via a subagent pass. |
| `new-feature` | End-to-end workflow for adding a new feature from a task description, layer by layer, with validation and documentation updates. |
| `refactor` | Improve code structure without changing behavior using safe, test-preserving refactoring steps with a verified safety net. |

---
*Last regenerated by `skill-author`. The canonical source is the `SKILL.md` files under `template/.claude/skills/`.*
